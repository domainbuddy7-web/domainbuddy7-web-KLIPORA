"""
Panel modules: one file per dashboard panel (Status, Video Factory, etc.).

Each panel exposes build() and optionally registers callback handlers
with the router. No code moved here yet; structure only.
"""
